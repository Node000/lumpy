class_name WallTag
extends Node
## Marker that the level builder can hang on walls to mark them smooth.
## Placed on the level root as wall_smooth<|idx> nodes (no physics attached).

var smooth := false
